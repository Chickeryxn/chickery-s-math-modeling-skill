{
  "schema_version": 2,
  "description": "Human decision provenance contract.",
  "required": ["decision_id", "decision_type", "status", "source", "choice", "evidence_refs", "recorded_at"],
  "status_values": ["SUGGESTED", "PENDING", "DECIDED", "SUPERSEDED", "STALE"],
  "source_required_for_decided": ["source_type", "user_message_id", "user_verbatim_answer"],
  "evidence_reference_policy": "Project-relative file paths are resolved from PROJECT_ROOT; evidence:<id> is allowed only for explicitly registered external/user evidence.",
  "rules": [
    "Only source_type=user_answer may be DECIDED.",
    "AI summaries must be stored separately from user_verbatim_answer.",
    "A superseding record must point to an earlier record and append rather than overwrite.",
    "Project-relative evidence paths must resolve to files inside PROJECT_ROOT."
  ]
}
