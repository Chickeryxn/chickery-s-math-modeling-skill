# assets/ —— 其它素材

放训练所需的其它素材：赛题文件（`.txt/.pdf`，供 `planning/training_config.json` 的 `problem_source` 指向）、题解范例、可视化数据等。规则同其它目录：素材 + 说明，登记进 index.json。

建议：把训练赛题统一放这里（如 `assets/problems/`），`training_config.json` 的 `problem_source` 指向即可。
