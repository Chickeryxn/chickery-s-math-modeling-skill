# problems（训练赛题占位目录）

`planning/training_config.json` 的 `problem_source` 默认指向本目录。

- 把训练赛题以 `resources-library` 允许的格式放入：`<题目>.txt`（或题目附件压缩包），例如 `2024C.txt`。
- 也可改 `training_config.json` 的 `problem_source` 指向其它位置。
- 本目录内容版权归赛题出处所有，仅供个人训练使用；不要把受版权保护的赛题推送进公开仓库。
